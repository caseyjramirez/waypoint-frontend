const baseUrl = 'http://localhost:4200'

const userAPI = `${baseUrl}/api/user`
const loginAPI = `${userAPI}/login`
const getUserDataAPI = `${userAPI}/data`


export {userAPI, loginAPI, getUserDataAPI}